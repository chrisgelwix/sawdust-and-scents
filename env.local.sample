NODE_ENV=development

# Default database configuration for local development
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=sdas_user
POSTGRES_PASSWORD=sdas_password
POSTGRES_DB=sdas_db

MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_USER=sdas_admin
MONGO_PASSWORD=sdas_password
MONGO_DB=sdas_mongodb

# Keycloak configuration (placeholders - update with actual values if needed)
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=sdas
KEYCLOAK_CLIENT_ID=sdas-api
KEYCLOAK_CLIENT_SECRET=change_me
